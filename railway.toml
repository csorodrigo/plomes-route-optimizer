[build]
builder = "NIXPACKS"
buildCommand = "rm -rf node_modules/.cache && npm ci --omit=dev && cd frontend && rm -rf node_modules/.cache && npm ci --omit=dev && DISABLE_ESLINT_PLUGIN=true CI=false npm run build"
watchPatterns = ["**/*.js", "**/*.jsx", "**/*.ts", "**/*.tsx", "**/*.json", "frontend/src/**/*"]

[deploy]
startCommand = "NODE_ENV=production node backend/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"